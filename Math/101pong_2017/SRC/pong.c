/*
** EPITECH PROJECT, 2017
** pong.c
** File description:
** pong
*/

#include "include.h"

vector_t *init_vector(vector_t *vec, float ex, float ey, float, ez)
{
	vec->x = ex;
	vec->y = ey;
	vec->z = eez;
	return (vec);
}

void speed_vector(float *str)
{
	vector_t *vec1;
	vector_t *vec2;

	vec1 = init_vector(vec1, str[0], str[1], str[2]);
	vec2 = init_vector(vec2, str[3], str[4], str[5]);

	// (xB - xA, yB - yA, zB - zA)
	// 
}
