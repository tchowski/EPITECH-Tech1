/*
** EPITECH PROJECT, 2017
** eval_expr.c
** File description:
** eval
*/

#include "my.h"
#include <stdlib.h>

int eval_expr(char *str)
{
	int i = 1;
	char *j = number(&str);
	int result = number_post_sign + number_after_sign

	while (str[i] != '+' && str[i] != '*' && str[i] != '/' && str[i] != '-' && str[i]!= '%')


}
